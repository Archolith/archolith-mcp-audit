{
  "name": "archolith-audit",
  "version": "0.1.0",
  "description": "See exactly how much of your context window each MCP server is consuming. Tracks per-server token spend, detects waste patterns (polling loops, oversized results, redundant fields), and surfaces concrete optimization suggestions — live, during every Claude Code session.",
  "author": {
    "name": "archolith"
  },
  "homepage": "https://archolith.dev",
  "repository": "https://github.com/Archolith/archolith-audit-plugin-claude",
  "license": "Apache-2.0",
  "keywords": ["mcp", "audit", "tokens", "telemetry"],
  "mcpServers": {
    "archolith-audit": {
      "command": "python",
      "args": ["-m", "archolith_mcp_audit.mcp_server"],
      "env": {
        "MCP_AUDIT_ENABLED": "1",
        "MCP_AUDIT_SESSION_ID": "${CLAUDE_SESSION_ID}",
        "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
